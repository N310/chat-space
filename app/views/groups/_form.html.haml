- if group.errors.any?
  .chat-group-form__errors
    %h2
      #{group.errors.count}件のエラーが発生しました。
    %ul
      - group.errors.full_messages.each do |message|
        %li #{message}

= form_for group do |f|
  .chat-group-form__field.clearfix
    .chat-group-form__field--left
      = f.label :name, "グループ名", class: "chat-group-form__label", for: "chat_group_name"
    .chat-group-form__field--right
      - if params[:action] == "edit"
        = f.text_field :name, placeholder: "グループ名を入力してください", id: "chat_group_name", class: "chat-group-form__input", value: "test value"
      - else
        = f.text_field :name, placeholder: "グループ名を入力してください", id: "chat_group_name", class: "chat-group-form__input"
  .chat-group-form__field.clearfix
    / この部分はインクリメンタルサーチ（ユーザー追加の非同期化のときに使用します
    /
      <div class='chat-group-form__field--left'>
      <label class="chat-group-form__label" for="chat_group_チャットメンバーを追加">チャットメンバーを追加</label>
      </div>
      <div class='chat-group-form__field--right'>
      <div class='chat-group-form__search clearfix'>
      <input class='chat-group-form__input' id='user-search-field' placeholder='追加したいユーザー名を入力してください' type='text'>
      </div>
      <div id='user-search-result'></div>
      </div>
  .chat-group-form__field.clearfix
    .chat-group-form__field--left
      = f.label :member, "チャットメンバー", class: "chat-group-form__label", for: "chat_group_member"
    .chat-group-form__field--right
      = f.collection_check_boxes(:user_ids, User.all, :id, :name ) do |e|
        = e.label { e.check_box + e.text }
      / この部分はインクリメンタルサーチ（ユーザー追加の非同期化のときに使用します
      /
        <div id='chat-group-users'>
        <div class='chat-group-user clearfix' id='chat-group-user-22'>
        <input name='chat_group[user_ids][]' type='hidden' value='22'>
        <p class='chat-group-user__name'>seo_kyohei</p>
        </div>
        </div>
  .chat-group-form__field.clearfix
    .chat-group-form__field--left
    .chat-group-form__field--right
      = f.submit :commit , value: "Save", class: "chat-group-form__action-btn", data: { disable_with: "Save" }
